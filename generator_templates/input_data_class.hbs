/**
 * Represents request frame that can be used with {{Domain}}.{{Name}} method call.
 *
 *{{{trim Description}}}
 */
data class {{SimpleName}}Request (
    {{#each Parameters}}
    /**
     *{{{trim Description}}}
     */
    {{#if Experimental}}@{{Package}}.Experimental {{/if}}{{#if Deprecated}}@{{Package}}.Deprecated {{/if}}{{{value Name}}}: {{{GetFormattedType}}}{{#if Optional}}? = null{{/if}}{{#unless @last}},{{/unless}}

    {{/each}}
)
